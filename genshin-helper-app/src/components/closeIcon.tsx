export default function CloseIcon(props: { className: string }) {
    return (
        <svg className={props.className} viewBox="0 0 40 40">
            <path d="M 10,10 L 30,30 M 30,10 L 10,30" stroke-width="4" />
        </svg>
    );
}
